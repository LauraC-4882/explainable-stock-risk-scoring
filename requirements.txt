yfinance>=0.2.40
akshare>=1.18
pandas>=2.0
pyarrow>=14.0  # parquet engine for the snapshot fallback — used to ride in transitively via streamlit
numpy>=1.26
scikit-learn>=1.7,<1.8  # models/artefacts pickle is from 1.7.x; 1.9.0 broke unpickling in CI (SimpleImputer._fill_dtype)
xgboost>=2.0,<3.0  # shap 0.49.1 can't parse XGBoost 3.x's base_score serialization format
shap==0.49.1  # pinned exactly, not just >=0.45 — see CLAUDE.md for why
arch>=6.3
ta>=0.11.0
pandera[pandas]>=0.20
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
pydantic-settings>=2.2
email-validator>=2.0
sqlmodel>=0.0.16
pyjwt>=2.8
bcrypt>=4.0
httpx>=0.27
prometheus-client>=0.20
scipy>=1.13
joblib>=1.4
PyYAML>=6.0
python-dotenv>=1.0
loguru>=0.7
cachetools>=5.3
gradio>=5.0  # ui/gradio_app.py — [F3]'s Hugging Face Space (full ML+SHAP demo)
